{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/**/*.py": {
      "includeFiles": "api/**/*.{pkl,onnx,csv,safetensors,json}"
    }
  }
}
